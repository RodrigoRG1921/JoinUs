import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  view: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  image: {
    alignContent: 'center',
    marginRight: 26,
    width: 50,
    height: 40,
  },
})

export {styles}
